import React from 'react'

const Card = ({h2, h3}) => {
    return (
        <div>
            <h2>Fist card's h2 {h2}</h2>
            <h3>Third card's {h3}</h3>
        </div>
    )
}

export default Card